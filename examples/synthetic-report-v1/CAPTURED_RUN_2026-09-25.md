# Captured Docling run — 25 September 2026

This is the first archived output for the synthetic fixture. The [raw Markdown](captured-output/docling-2.130.0.md) and [raw Docling JSON](captured-output/docling-2.130.0.json) were copied without editing from the local run. They contain synthetic content only; a check found no absolute home paths or key-shaped strings in either file.

| Item | Recorded value |
| --- | --- |
| Input | `input/sample_report.pdf`, generated by `generate.py` |
| Input SHA-256 | `d330cb5cd9af95ca083cbd68332f8049e633d7bcdf8933b798f42428e0f12fc3` |
| Environment | macOS arm64, Python 3.11.15 |
| Main packages | Docling 2.130.0; Docling Core 2.98.1; rapidocr-onnxruntime 1.4.4; OpenCV headless 5.0.0.93; torch 2.14.0; transformers 5.17.0 |
| Setup | Fresh virtual environment; installed `requirements-dev.txt` with `uv pip install` |
| Command | `python src/ocr_llm_pipeline.py --input-dir examples/synthetic-report-v1/input --markdown-dir md_results/synthetic-report-v1` |
| External services | No LLM call or API key; model weights downloaded on first run |

The command completed and wrote Markdown and JSON. The raw JSON has **zero detected tables**; its three text elements include a paragraph that flattens the region, Q1, and Q2 values. All six numbers appear in the Markdown, but row and column associations are not represented as a table. This is a documented structural failure for this synthetic input, not a measured OCR accuracy or speed result. The PDF is native text, so it also does not qualify scanned-document OCR. Future Docling/model versions may produce different output; retain raw output and versions when comparing.
